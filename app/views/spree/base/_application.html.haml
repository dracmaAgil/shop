= render partial: 'spree/shared/header'

#wrapper.row{"data-hook" => ""}
  / = taxon_breadcrumbs(@taxon)
  / = render partial: 'spree/shared/sidebar' if content_for? :sidebar
  #content{:class => "container-fluid columns #{!content_for?(:sidebar) ? "sixteen" : "twelve omega"}", "data-hook" => ""}
    = flash_messages
    = yield
  = yield :templates